#include "main.h"

/**
 * main - Entry point.
 * print_alphabet - Print lowercase alphabet using function stored in main.h.
 *
 * Return: absolute 0 (success)
 */

int main(void)
{
	print_alphabet();
	return (0);
}
