#include "main.h"

/**
 * _strlen - function to return the length of a string
 * @s: The string variable
 * Return: string length
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; i <= s[i]; i++)
	{
		i = i;
	}
	return (i);
}
