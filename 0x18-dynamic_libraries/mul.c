#include "main.h"

/**
 * mul - function that multiply two numbers
 * @a: first integer
 * @b: second integer
 * Return: multiplication value of a and b
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
