#include "main.h"

/**
 * mod - function that find the mod two numbers
 * @a: first integer
 * @b: second integer
 * Return: module of a and b
 */

int mod(int a, int b)
{
	int c;

	c = a % b;
	return (c);
}
